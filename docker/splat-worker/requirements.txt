# Pinned runtime dependencies (resolved with `uv pip compile`, Python 3.12; same pins as wall-geometry
# where shared). Top-level: fastapi uvicorn python-multipart numpy pillow.
# The protocol package docker/compute-jobs-py is copied in by the Dockerfile / put on PYTHONPATH by
# run-native.sh (no install step).
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
click==8.5.0
fastapi==0.141.1
h11==0.16.0
idna==3.20
numpy==2.5.3
pillow==12.3.0
pydantic==2.13.5
pydantic-core==2.46.5
python-multipart==0.0.32
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
uvicorn==0.53.0
